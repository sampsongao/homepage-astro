---
const { height, width, heightRatio, widthRatio } = Astro.props;
---

<div class="relative" style={{ height, width, paddingTop: `calc(${widthRatio} / ${heightRatio} * 100%)` }}>
  <div class="absolute top-0 bottom-0 left-0 right-0">
    <slot />
  </div>
</div>
